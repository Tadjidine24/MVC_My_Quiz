{% extends 'base.html.twig' %}

{% block body %}
{% if question %}
	<table id="question" class="table table-striped">
		<thead>
			<tr>
				<th>
					Question
				</th>
			</tr>
		</thead>
		<tbody>
			{% for question in question %}
				<tr>
					<td>
                        {{ question.id }}. {{ question.question }}
					</td>
					<td>
						<a href="/question/{{ question.id }}">
							<button type="button" class="btn btn-info">
								Détails
							</button>
						</a>
						<a href="/question/edit/{{ question.id }}">
							<button type="button" class="btn btn-warning">
								Modifier
							</button>
						</a>
						<a href="/question/delete/{{ question.id }}" onclick="return confirm('Etes-vous sûr de supprimer	cet	article?');">
							<button type="button" class="btn btn-danger">
								Supprimer
							</button>
						</a>
                    </td>
				</tr>

		</tbody>
	</tr>
</td></tbody></tr>{% endfor %}</tbody></table>{% else %}<p>Aucune question</p>{% endif %}{% endblock %}
